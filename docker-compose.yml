services:
  someday:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/someday.db
      - NODE_ENV=production
    restart: unless-stopped
